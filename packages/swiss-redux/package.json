{
  "name": "swiss-redux",
  "version": "1.0.0",
  "description": "Provides Redux hooks for Swiss.",
  "module": "module/swiss-redux.js",
  "main": "dist/swiss-redux.js",
  "files": [
    "module",
    "dist",
    "src"
  ],
  "scripts": {
    "lint": "eslint src",
    "prepublishOnly": "yarn lint && (cd ../.. && yarn build)"
  },
  "peerDependencies": {
    "swiss": "^1.0.0"
  }
}
