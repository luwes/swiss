{
  "name": "swiss-redux",
  "version": "1.0.0",
  "description": "Provides Redux hooks for Swiss.",
  "module": "dist/swiss-redux.mjs",
  "main": "dist/swiss-redux.js",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "lint": "eslint src",
    "prepublishOnly": "yarn lint && (cd ../.. && yarn build)"
  },
  "peerDependencies": {
    "swiss": "^1.0.0"
  }
}
