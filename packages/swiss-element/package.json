{
  "name": "swiss-element",
  "version": "0.10.1",
  "description": "Core swiss element library",
  "module": "dist/swiss-element.mjs",
  "main": "dist/swiss-element.js",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "lint": "eslint src",
    "prepublishOnly": "yarn lint && (cd ../.. && yarn test --single-run && yarn build)"
  }
}
