{
  "name": "swiss-hooks",
  "private": true,
  "version": "1.0.0",
  "description": "Hooks enhancer for Swiss.",
  "module": "../module/swiss-hooks.js",
  "main": "../dist/swiss-hooks.js",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "lint": "eslint src",
    "prepublishOnly": "yarn lint && (cd ../../.. && yarn build)"
  }
}
